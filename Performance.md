# Performance — замеры пропускной способности

Замер 2026-09-25. Задача: понять, сколько даёт сам транспорт MultiLink (10 UDP-дыр) по
сравнению с «голым» каналом, до того как поверх пойдёт WireGuard.

## Стенд

- **Отправитель** — VPS (Debian 12, 1 vCPU, 708 МБ ОЗУ), публичный IP на интерфейсе.
- **Приёмник** — домашний Linux-ПК за двумя NAT: домашний роутер (OpenWrt) и роутер
  провайдера. Маршрут до VPS проверен трассировкой (UDP и TCP): мимо VPN роутера, через WAN.
- Направление во всех замерах: VPS → домашний ПК (как скачивание).
- Пакет 1392 байта — ровно пакет WireGuard при MTU туннеля 1360.
- На VPS включён `ufw` (входящий UDP запрещён). Порты дыр случайные, поэтому пакеты
  пробива от дома резались (`UFW BLOCK` в журнале ядра), а дыры держались плохо. На время
  замера входящий UDP был разрешён одним временным правилом `iptables` поверх `ufw`
  (без `ufw disable`: NAT для VPN на этом VPS живёт в `/etc/ufw/before.rules`).

## Базовая линия (iperf 2)

| Замер | Результат |
|---|---|
| TCP VPS → дом | 88,2 Мбит/с |
| TCP дом → VPS | 81,5 Мбит/с |
| TCP VPS → дом, 4 потока | 81,5 Мбит/с (потолок канала) |
| UDP VPS → дом, 30 Мбит/с | 31,5 Мбит/с, потерь 0,01% |
| UDP VPS → дом, 60 Мбит/с | 62,9 Мбит/с, потерь 0% |
| UDP VPS → дом, 90 Мбит/с | 93,8 Мбит/с, потерь 0,003% |

## MultiLink без WireGuard (`mlbench`)

`hp-backend/peer/src/bin/mlbench.rs`: отправитель гонит через `MultiLink::send_data` пакеты
фиксированного размера с заданной скоростью, приёмник считает скорость, потери и долю пакетов
не по порядку. Порядок считается по **собственному номеру `mlbench`** в теле пакета (байты
8–15 после метки `MLBN`), а не по WireGuard. Буфер порядка (`reorder`) выключен
(`reorder_wait = 0`), чтобы видеть сырую картину. 10 живых дыр, 10 с на скорость, свежие GUID
на каждый прогон.

| Отправка | Принято | Потери | Не по порядку |
|---|---|---|---|
| 30 Мбит/с | ~30 Мбит/с | 0% | 79% |
| 60 Мбит/с | 59,9 Мбит/с | 0% | 76% |
| 90 Мбит/с (отправлено 89,9) | 80,9 Мбит/с | 10% | 80% |

## Телефон без WireGuard (`mlbench` на Android)

Тот же `mlbench`, собранный под `armv7-linux-androideabi` (NDK), приёмник на телефоне
(Android, 4 ядра MT6761, Wi-Fi 5 ГГц, линк 433 Мбит/с), отправитель на VPS, 8 с на скорость.

| Отправка | Принято | Потери | Не по порядку |
|---|---|---|---|
| 10 Мбит/с | 7,5 Мбит/с | 25,9% | 61% |
| 20 Мбит/с | 18,6 Мбит/с | 7,6% | 63% |
| 30 Мбит/с | 28,4 Мбит/с | 5,7% | 69% |
| 60 Мбит/с | 57,6 Мбит/с | 4,0% | 79% |
| 90 Мбит/с | 89,9 Мбит/с | 0,1% | 83% |

Транспорт на телефоне тоже тянет до ~90 Мбит/с. Потери на малых скоростях в этой серии в
следующей (ниже) не повторились (0% на 10 и 30 Мбит/с); вероятно, первая пачка шла сразу после
поднятия дыр. Причину не проверяли.

## Отставание переставленных пакетов

Чтобы выбрать время ожидания буфера порядка, `mlbench` меряет для каждого переставленного
пакета **отставание**: время от момента, когда заметили пробел (пришёл номер больше
ожидаемого), до прихода пропущенного пакета. Ровно столько буферу пришлось бы его ждать.
Буфер при этом выключен, порядок считается по номеру `mlbench`.

Приёмник — домашний ПК (проводная сеть):

| Скорость | Потери | Не по порядку | Медиана | p90 | p99 | Макс |
|---|---|---|---|---|---|---|
| 10 Мбит/с | 0% | 50% | 2,3 мс | 8,5 мс | 20,9 мс | 47,3 мс |
| 30 Мбит/с | 0% | 73% | 2,6 мс | 9,0 мс | 11,3 мс | 45,7 мс |
| 60 Мбит/с | 0% | 79% | 2,8 мс | 9,9 мс | 11,3 мс | 14,3 мс |
| 90 Мбит/с | 0% | 82% | 2,9 мс | 9,9 мс | 11,6 мс | 17,0 мс |

Приёмник — телефон (Wi-Fi 5 ГГц):

| Скорость | Потери | Не по порядку | Медиана | p90 | p99 | Макс |
|---|---|---|---|---|---|---|
| 10 Мбит/с | 0% | 54% | 3,1 мс | 9,8 мс | 15,1 мс | 21,6 мс |
| 30 Мбит/с | 0% | 75% | 3,5 мс | 10,4 мс | 14,8 мс | 23,8 мс |
| 60 Мбит/с | 2,1% | 83% | 3,1 мс | 12,0 мс | 28,7 мс | 82,4 мс |
| 90 Мбит/с (принято 76,7) | 14,6% | 86% | 5,0 мс | 24,3 мс | 48,6 мс | 237,7 мс |

Что это значит для `reorder_wait`:

- Текущие 8 мс покрывают медиану, но не p90: около 10% переставленных пакетов буфер не
  дождётся и выдаст по таймауту.
- На проводе p99 ~11–12 мс на рабочих скоростях: ожидание **~12–15 мс** покрыло бы ~99%.
- На телефоне под нагрузкой хвост длинный (p99 29–49 мс, макс сотни мс): фиксированное
  значение либо не дожидается, либо тормозит поток на каждой настоящей потере.
- Отсюда **адаптивное ожидание** (сделано, см. ниже): буфер сам меряет отставание и ставит
  ожидание по скользящему p99 в пределах 3–30 мс.

## Выводы

- До 60 Мбит/с MultiLink даёт полную скорость канала без потерь. Потолок около 80 Мбит/с
  против ~94 у iperf UDP; где теряются 10% на 90 Мбит/с (отправка, сеть или приём на
  одноядерном VPS), не выяснено.
- Ранний замер «телефон → WireGuard → MultiLink → VPS» давал 8–10 Мбит/с. Это не предел
  транспорта: узкое место выше (телефон, WireGuard в пространстве пользователя, мост в
  приложении, перемешивание для вложенного TCP).
- **Перемешивание 75–80%** — прямое следствие распределения пакетов по 10 дырам. Для
  WireGuard само по себе не страшно, но вложенный TCP принимает перестановки за потери: на
  телефоне 62–75% входящих TCP-сегментов шли вне очереди (`TCPOFOQueue`).

## Восстановление порядка (`reorder`)

`hp-backend/connection/src/reorder.rs`, включается в `MultiLink` на приёме. Наш протокол не
меняется: у пакета передачи данных WireGuard (тип 4) в открытом заголовке лежат номер сессии
(`receiver index`) и 64-битный счётчик. Буфер выдаёт пакеты по счётчику, недостающий ждёт не
дольше текущего ожидания, потом отдаёт накопленное; опоздавший пакет
передаётся дальше (WireGuard принимает пакеты вне очереди в пределах своего окна). Состояние
отдельно на каждую сессию WireGuard, буфер ограничен.

**Адаптивное ожидание.** Буфер меряет отставание каждого восстановленного пробела (от момента,
когда пробел заметили, до прихода недостающего пакета) и опоздавших (выданных по таймауту и
пришедших позже: у них полное время с момента обнаружения, иначе ожидание не смогло бы
расти). По последним 512 отсчётам раз в 64 отсчёта берётся p99, к нему +25% запаса, и
ожидание ставится в пределах **3–30 мс**. Начальное значение — `reorder_wait` (8 мс).

Настройки: `REORDER_WAIT_MS` (начальное ожидание) в `server`, `--ei reorderMs` в Android-приложении, в `client` —
переменная окружения; 0 выключает. Раз в 30 с в лог (`INFO`) пишутся текущее ожидание и счётчики: по порядку,
переставлено, по таймауту, опоздавших, принудительно, прочих, макс. придержано. Каждое
событие перестановки видно на `TRACE` (`RUST_LOG=connection::reorder=trace`): пакет
придержан, пришёл недостающий, не дождались за N мс, опоздавший, смена ожидания.

Эффект `reorder` на реальной сети ещё не измерен (в замерах `mlbench` буфер выключен, а пакеты
`mlbench` не похожи на WireGuard, так что буфер их всё равно пропустил бы как «прочие»).

Ещё одна опция для замеров: `data_holes` (`DATA_HOLES`, `--ei dataHoles`) — слать данные только
через N дыр (1 — через одну, без перемешивания).

## Роутер OpenWrt: где упирается процессор (2026-09-26)

Роутер Xiaomi Mi Router 4C (MIPS, одно ядро MT7628, 580 МГц, OpenWrt 23.05) как клиент
VPS-сервера (`vps-client`, схема «клиент — сервер», см. `OpenWRT/Wireguard.md`). Весь LAN роутера
через WireGuard поверх дыр давал 7–9 Мбит/с (напрямую через WAN роутера — 68–88 Мбит/с), процессор
на 100%. Чтобы понять, что именно его ест, — два замера без WireGuard.

**Кодек** (`mlbench codec`, пакет `Data` 1403 байта, мкс на пакет):

| Операция | MT7628 (роутер) | x86-64 (ПК) |
|---|---|---|
| XOR первых 64 байт | 3 | 0,01 |
| XOR всего пакета (для сравнения) | 33 | 0,04 |
| encode (protobuf + XOR) | 13 | 0,09 |
| decode (в т.ч. копия буфера 3,5 мкс) | 20 | 0,20 |
| потолок одного кодека | ~30 000 пакетов/с, ~340 Мбит/с | ~3,4 млн пакетов/с |

На 8 Мбит/с (~700 пакетов/с) кодек занимает ~2% процессора роутера: XOR и protobuf не виноваты.
Маскировка только 64 байт вместо всего пакета экономит 30 мкс на пакет.

**MultiLink без WireGuard** (`mlbench`, VPS → роутер, 10 дыр, 8 с на скорость) — рядом тот же
замер с приёмником на бюджетном Android-телефоне (ARM, 4 ядра MT6761, Wi-Fi 5 ГГц):

| Отправка | Роутер MT7628: принято | потери | процессор | Телефон MT6761: принято | потери |
|---|---|---|---|---|---|
| 5 Мбит/с | 5,0 | 0% | idle 45% | — | — |
| 10 Мбит/с | 10,0 | 0% | idle 18% | 7,5 | 25,9%* |
| 20 Мбит/с | 20,0 | 0% | idle 0% (usr 54%, sys 45%) | 18,6 | 7,6%* |
| 30 Мбит/с | — | — | — | 28,4 | 5,7%* |
| 40 Мбит/с | 33,4 | 15,9% | idle 0% | — | — |
| 60 Мбит/с | — | — | — | 57,6 | 4,0% |
| 90 Мбит/с | — | — | — | 89,9 | 0,1% |

\* в повторной серии на телефоне потерь на 10 и 30 Мбит/с не было (см. «Отставание переставленных
пакетов»): первая пачка шла сразу после поднятия дыр.

Выводы:

- **Телефон на ARM такой проблемы не имеет:** тот же код принимает ~90 Мбит/с, упор там в канал, а не
  в процессор. Узкое место — именно слабый одноядерный MIPS роутера.
- На роутере сам транспорт тянет ~20 Мбит/с без потерь и ~33 максимум. С WireGuard процессор делят
  шифрование в ядре (~45%) и `vps-client` (~45%, плюс мост к `127.0.0.1:51820` и ответы TCP), отсюда
  7–9 Мбит/с.
- Время `vps-client` уходит не на кодек, а на обработку пакета: по одному системному вызову
  `recvfrom`/`sendto` на пакет (~40% `sys`), переходы между задачами tokio (приём дыры → канал событий
  → буфер порядка → канал входящих → мост) и выделение `Vec` на каждом шаге.
- Резервы (не сделано): пакетные `recvmmsg`/`sendmmsg`, передача `Data` в мост без лишних каналов,
  разбор без копирования нагрузки. Отказ от шифрования WireGuard дал бы на этом роутере максимум ~2×
  ценой скрытности (SNI и DNS внутри туннеля станут видны), поэтому не рассматривается.
- Микробенч кодека на телефоне не снимали (телефон не был подключён).

## Роутер: однопоточный tokio, банк буферов, быстрый кодек (2026-09-26)

Разбор по фактам (`strace -c` под нагрузкой и «голый» UDP для сравнения) показал, что ядро и сеть
OpenWrt не узкое место: блокирующий UDP-сокет без tokio принимает на этом роутере 80 Мбит/с без
потерь (idle 13%, на 20 Мбит/с idle ~80%). Время съедал наш процесс:

| Шаг | Что было в `strace -c` (10 с, ~9000 пакетов) |
|---|---|
| многопоточный tokio на одном ядре | `futex` 17 875 вызовов по 123 мкс — 85% времени системных вызовов (пробуждения потоков, ~2 на пакет) |
| однопоточный tokio | `futex` исчез; `mmap`/`munmap` по ~4500 (malloc musl отдаёт память ядру на каждом втором пакете) |
| банк буферов + быстрый кодек | остались `recvfrom` и `epoll_pwait`; время в системных вызовах 0,33 с вместо 2,58 с |

Что сделано (без 64-битных атомиков и сторонних аллокаторов):

- **Банк буферов** (`connection/src/pool.rs`): 100 буферов по 1500 байт на процесс, пакет возвращает
  буфер при дропе; нагрузка от дыры до моста идёт в нём.
- **Быстрый кодек** (`connection/src/wire.rs`): `Data` и `WrappedData` собираются прямо в буфер на
  стеке и разбираются без копии; формат — тот же protobuf, байт-в-байт с prost (тесты). Остальные
  сообщения — prost.
- Приём дыры снимает маску на месте (без `to_vec`), отправка и мосты работают со срезами, выбор
  дыры без выделений памяти (xorshift32 вместо `getrandom` на пакет), буфер порядка — кольцо
  фиксированного размера вместо `BTreeMap` и переиспользуемый `Vec` выдачи.
- `vps-client` и `vps-server` по умолчанию на однопоточном tokio (`RUNTIME=multi` — многопоточный).

**Кодек на роутере** (`mlbench codec`, мкс на пакет 1403 байта):

| | encode | decode |
|---|---|---|
| prost | 12,7 | 19,9 |
| быстрый путь (`wire` + банк) | 7,7 | 10,6 (в т.ч. тестовая копия ~3,5) |

**MultiLink без WireGuard на роутере** (`mlbench`, VPS → роутер, 10 дыр, 8 с на скорость, idle —
свободный процессор роутера):

| Отправка | многопоточный tokio, до | однопоточный, до | однопоточный + банк + `wire` |
|---|---|---|---|
| 10 Мбит/с | 10,0, idle 18% | 10,0, idle 36% | 10,0, idle 72% |
| 20 Мбит/с | 20,0, idle 0% | 20,0, idle 7% | 20,0, idle 50% |
| 40 Мбит/с | 32,6, потери 17,7% | 39,8, потери 0,4% | 40,0, потери 0% (idle 7%) |
| 60 Мбит/с | 22,4, потери 62,1% | 44,4, потери 25,4% | 58,4, потери 2,6% |

**WireGuard через роутер** (ноутбук в LAN → роутер `wg0` → `vps-client` → дыры → `vps-server` →
WireGuard VPS, iperf TCP):

| | VPS → клиент | клиент → VPS |
|---|---|---|
| до | 7,3 Мбит/с | 9,5 Мбит/с |
| после | 10,2 Мбит/с | 12,1 Мбит/с |
| напрямую через WAN роутера, без туннеля | 68,0 Мбит/с | 88,4 Мбит/с |

Процессор роутера по-прежнему загружен на 100%, но теперь на нашу программу (`usr`) приходится
8–25%, а `sys` 41–58% и `softirq` 33%: шифрование WireGuard в ядре (ChaCha20 на MIPS без SIMD),
лишний UDP-переход `vps-client` ↔ WireGuard через `127.0.0.1` и пересылка/NAT трафика LAN.
Дальнейшие резервы: пакетные `recvmmsg`/`sendmmsg`, TUN вместо WireGuard (ценой шифрования),
роутер мощнее.

