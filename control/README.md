# control — управление home-proxy: трей, сопряжение по QR

**Статус: описание. Кода пока нет.** Здесь зафиксирован замысел, чтобы `server`, `router`,
трей и Android-приложение сходились на одном протоколе.

`control/` (трей-приложение) лежит на одном уровне с `server` и `router`, а не внутри них:
`server` и `router` остаются самостоятельными службами, их можно настраивать вручную (`.env`, `server.env`) и
запускать вообще без `control`. Связь между ними одна: TCP-протокол управления (ниже).

## Что это

- **Протокол управления** — «пульт» службы: статус дыр, события, свои GUID, смена пира,
  перезапуск.
- **Трей-приложение** (Windows, Linux) на Slint: значок в области уведомлений, окно с
  состоянием и **показом QR-кода** для сопряжения.
- **Сопряжение с Android**: телефон сканирует QR и получает всё нужное (GUID'ы, адреса
  STUN/MQTT, ключи WireGuard) без `adb` и ручного ввода.

## Протокол: тот же protobuf, но без шифрования

Кадр: `u32` (big-endian, длина) + protobuf `ControlMessage` (`oneof`). Используется тот же
`prost`, что и в `connection` (новых зависимостей на роутере нет; на OpenWrt каждый
килобайт на счету).

**Чем отличается от протокола дыр (`PeerMessage`).** Там поверх protobuf идёт XOR
циклическим вектором: это маскировка от DPI на пути через интернет. Здесь XOR нет: канал
работает в **«демилитаризованной зоне»**, то есть только между доверенными узлами и не
выходит в интернет. Маскировать нечего, а слой XOR дал бы лишь ложное чувство защиты.

Что считается «DMZ» — на какой адрес слушает служба (`CONTROL_ADDR`):

| Где | Адрес | Замечание |
|---|---|---|
| ПК, `server` и трей на одной машине | `127.0.0.1:47001` | по умолчанию |
| Windows-трей + `server` в WSL2 | `127.0.0.1:47001` | WSL2 пробрасывает localhost, см. ниже |
| Роутер с OpenWrt | адрес LAN-интерфейса, например `192.168.1.1:47001` | **не** `0.0.0.0` и не WAN |

Пробрасывать unix-сокет из WSL не нужно: TCP на loopback одинаков во всех случаях, в
трее меняется только адрес.

**Защита.** Шифрования нет, поэтому:
- слушаем только нужный интерфейс, не `0.0.0.0`; на роутере правило брандмауэра тоже
  запрещает порт из WAN;
- первое сообщение соединения — `Hello { token }` (токен генерирует служба при первом
  запуске и хранит рядом с настройками); без верного токена соединение закрывается;
- токен не пишем в логи; сравнение в постоянном времени;
- в LAN трафик управления виден другим узлам, поэтому сегмент должен быть доверенным. Если
  понадобится иное, поверх добавляется TLS, формат сообщений не меняется.

### Сообщения (эскиз)

```proto
message ControlMessage {
  oneof body {
    Hello        hello         = 1;   // token, версия протокола
    GetStatus    get_status    = 2;
    Status       status        = 3;   // состояние (rendezvous/punching/connected), дыры n/10, клиенты
    Subscribe    subscribe     = 4;   // присылать события
    Event        event         = 5;   // дыра открыта/потеряна, смена эндпоинта пира, ошибка
    GetIdentity  get_identity  = 6;   // мой GUID, GUID пира
    Identity     identity      = 7;
    SetPeer      set_peer      = 8;   // GUID пира (или пара для нового телефона)
    CreatePairing create_pairing = 9; // сгенерировать пакет сопряжения (см. ниже)
    Pairing      pairing       = 10;  // PairingBundle для QR
    Restart      restart       = 11;
  }
}
```

Соединение держится открытым: служба сама присылает `Event` после `Subscribe`. Трей не
опрашивает.

## Кто что делает

- **`server` (модуль `control`)** — единственный источник правды. Он:
  - при первом запуске генерирует свой GUID и токен управления и хранит их рядом с настройками;
  - по запросу трея создаёт **пакет сопряжения**: генерирует GUID телефона и ключи
    WireGuard, добавляет пир телефона и отдаёт `PairingBundle` (см. ниже);
  - слушает `CONTROL_ADDR` и сообщает трею статус и события (дыры, смена эндпоинта, ошибки).
- **`control/` (трей на Slint)** — тонкий клиент: подключается по TCP, получает от `server`
  GUID'ы и пакет сопряжения и показывает QR. Своих ключей и GUID он не генерирует и
  ничего не хранит.

Модуль `control` в `server` необязательный: включается настройкой `CONTROL_ADDR` (пусто =
выключен) и, при необходимости, фичей Cargo, чтобы сборка для роутера не тянула лишнего.
Без него `server` работает как раньше и настраивается вручную. Типы протокола и кадрирование
лежат в общей библиотечной части `control` и не зависят от `server`. Трей, в свою очередь, не
зависит от того, где работает `server`: локально, в WSL2 или на роутере, отличается только адрес.

Принятые решения:
- **`server` в целевой схеме работает на Linux**: в WSL2 на ПК, на роутере с OpenWrt или на
  обычном Linux. Нативная служба Windows (`windows/`) остаётся запасным вариантом и в схему
  `control` не входит.
- **Ключи WireGuard генерирует сам `server`** через `x25519-dalek` (секрет кладётся в
  `StaticSecret`, публичный ключ и приватный записываются в base64, как у `wg genkey`/`wg
  pubkey`). Зависимость от установленных утилит не нужна, а на роутере ключи так же
  создаются на месте. Случайность из ОС (`getrandom`). Бинарник вырастет на десятки КБ, на
  32-битном MIPS `curve25519-dalek` сам выбирает 32-битный бэкенд, 64-битные атомики не нужны.
- **Пир телефона добавляется командой `wg set`** (`wg set wghp peer <публичный ключ>
  allowed-ips <адрес>/32`): в WSL2 и на роутере это штатный `wg` из `wireguard-tools`.
  Конфиг (`wghp.conf`) при этом дописывается, чтобы пир пережил перезапуск.

Открытое: где `server` берёт список занятых адресов туннеля (для нового телефона нужен
следующий свободный `10.77.0.N/32`) — из `wg show wghp allowed-ips` или из собственного
файла состояния. Предпочтительнее `wg show`, тогда источник правды один.

## Трей на Slint

- Значок в области уведомлений: цвет по состоянию (нет связи / пробив / есть дыры).
  Трей рисует отдельный крейт (например, `tray-icon`), Slint отвечает за окно.
- Окно: состояние, число живых дыр, список телефонов, кнопка **«Добавить телефон»**.
- «Добавить телефон» показывает QR (ниже).
- Трей подключается по TCP к `CONTROL_ADDR` из своих настроек: локальный `server`, служба в
  WSL2 или роутер — отличаются только адресом.

## Сопряжение: QR-код и Android

Проблема: телефон должен узнать GUID пира, а пир — GUID телефона, до первой встречи. Чтобы
не нужен был обратный канал, **пакет сопряжения целиком собирает `server`** (как `gen.sh`),
трей получает его по протоколу управления и показывает одним QR.

### PairingBundle (protobuf, тот же формат)

```proto
message PairingBundle {
  uint32 version        = 1;
  string pc_guid        = 2;   // GUID службы (PEER_ID у телефона)
  string phone_guid     = 3;   // сгенерирован на ПК (MY_ID у телефона)
  string stun           = 4;   // ip:порт[,ip2:порт]
  string mqtt           = 5;   // ip:порт, TLS
  bytes  mqtt_ca_pem    = 6;   // ca.crt брокера
  string wireguard_conf = 7;   // client.conf: ключи телефона, адрес, DNS, MTU
  uint64 expires_unix   = 8;
}
```

Внутри лежит приватный ключ WireGuard телефона, поэтому пакет — **секрет**: показывается
только по кнопке, живёт несколько минут (`expires_unix`), после успешного сопряжения
недействителен, а служба на ПК одновременно добавляет пир телефона (публичный ключ и
`AllowedIPs`). QR не логируется и не сохраняется на диск.

Размер: пакет ~1,2 КБ (в основном CA-сертификат и конфиг WireGuard) — помещается в QR
(вместимость ~2,9 КБ в бинарном режиме), кодируется в base64url.

### Показ в трее

Трей получает `PairingBundle` от `server` (`CreatePairing` → `Pairing`) и строит QR (крейт `qrcode` или `fast_qr`, без сети) и рисует его в
окне Slint, с таймером до истечения.

### Сканирование на Android

- Отдельный экран «Сканировать» в `android-vpn`: камера + ZXing
  (`zxing-android-embedded`, работает без Google Play Services; альтернатива — ML Kit).
- Также deep link `homeproxy://pair?d=<base64url>`: QR можно открыть штатной камерой, а
  приложение подхватит ссылку.
- Разбор `PairingBundle` заполняет `Settings`: `myId`, `peerId`, `stun`, `mqtt`, `wgConfig`, CA
  (то, что сейчас делается аргументами `am start` и `wgConfigB64`). Приложение помнит, что
  сохранённый конфиг главнее `assets/wg.conf`, поэтому старый сбрасывается при сопряжении.
- После сопряжения запускаются дыры, VPN включается при первой живой дыре.

## Что дальше

1. `control`: крейт с `.proto`, кадрированием и клиентом; тесты на loopback.
2. Модуль `control` в `server` по `CONTROL_ADDR`: GUID, токен, статус, события, генерация
   пакета сопряжения.
3. Трей на Slint: значок, окно, QR.
4. Android: экран сканирования и разбор `PairingBundle`.
5. Добавление пира телефона на ходу при `CreatePairing` (`wg set` + дозапись в `wghp.conf`).
